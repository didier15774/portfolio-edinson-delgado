{
  "name": "portfolio-edinson-delgado",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:prod": "node scripts/build-prod.mjs",
    "preview": "astro preview --host",
    "check": "astro check",
    "test": "npm run build:prod && node --experimental-strip-types --test tests/**/*.test.ts",
    "test:unit": "node --experimental-strip-types --test tests/contact-validation.test.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.2.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@types/node": "^22.17.0",
    "sharp": "^0.35.4",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
